apiVersion: v1
kind: Pod
metadata:
  name: goserver
  labels:
    app: goserver
spec:
  containers:
    - name: goserver
      image: "fernandounuts/hello-go"
      # resources:
      #   requests:
      #     memory: "64Mi"
      #     cpu: "100Mi"
        
      #   limits:
      #     memory: "120Mi"
      #     cpu: "120Mi"